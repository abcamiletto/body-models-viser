[package]
name = "body-models-viser"
# Not published; the release version lives in pyproject.toml.
version = "0.0.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
glam = "0.32.1"
